apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models
  namespace: cozy-ai
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 30Gi
